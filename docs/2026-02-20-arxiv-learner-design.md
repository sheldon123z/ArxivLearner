# ArxivLearner - iOS arXiv 论文学习应用设计文档

> 日期: 2026-02-20
> 状态: 已确认
> 技术栈: SwiftUI 原生 (iOS 17+)
> 架构: 单体架构，按 Feature 分层
> 目标用户: 个人使用

---

## 1. 产品概述

一款 iOS 原生应用，用于在 arXiv 上搜索最新相关主题论文，通过卡片式交互浏览，并利用多种大模型进行深度学习辅助（摘要、问答、创新点提取、公式解析、翻译等）。

### 核心特性

- arXiv 论文搜索与发现
- 卡片式浏览（缩略卡片 → 完整卡片，支持翻转/展开）
- PDF 下载与内置阅读器
- doc2x PDF→Markdown 转换（提升 LLM 分析质量）
- 多 LLM 服务商接入（OpenAI / Anthropic / Gemini / DeepSeek / OpenRouter / 自定义中转站）
- 可定制 Prompt 模板系统
- 本地存储 + iCloud 同步

---

## 2. 整体架构

```
┌─────────────────────────────────────────────┐
│                  UI Layer (SwiftUI)          │
│  ┌─────────┐ ┌──────┐ ┌──────┐ ┌────────┐  │
│  │ Search  │ │Cards │ │Reader│ │  Chat  │  │
│  │  View   │ │ View │ │ View │ │  View  │  │
│  └────┬────┘ └──┬───┘ └──┬───┘ └───┬────┘  │
├───────┼─────────┼────────┼─────────┼────────┤
│              ViewModel Layer                 │
│         (ObservableObject / @Observable)     │
├──────────────────────────────────────────────┤
│               Service Layer                  │
│  ┌──────────┐ ┌──────────┐ ┌─────────────┐  │
│  │ ArxivAPI │ │LLMService│ │ Doc2xService│  │
│  └──────────┘ └──────────┘ └─────────────┘  │
├──────────────────────────────────────────────┤
│               Data Layer                     │
│  ┌──────────────────┐ ┌──────────────────┐   │
│  │    SwiftData     │ │  FileManager     │   │
│  │  (论文元数据/      │ │  (PDF/MD 缓存)   │   │
│  │   LLM见解/收藏)   │ │                  │   │
│  └────────┬─────────┘ └──────────────────┘   │
│           │ iCloud 同步 (CloudKit)            │
└───────────┴──────────────────────────────────┘
```

### 项目结构

```
ArxivLearner/
├── App/
│   └── ArxivLearnerApp.swift
├── Core/
│   ├── Network/
│   │   ├── ArxivAPIService.swift
│   │   └── HTTPClient.swift
│   ├── LLM/
│   │   ├── LLMServiceProtocol.swift
│   │   ├── LLMRouter.swift
│   │   ├── OpenAIService.swift
│   │   ├── AnthropicService.swift
│   │   ├── GenericOpenAIService.swift
│   │   └── ContextBuilder.swift
│   ├── Services/
│   │   └── Doc2xService.swift
│   ├── Storage/
│   │   ├── Models/
│   │   │   ├── Paper.swift
│   │   │   ├── ChatMessage.swift
│   │   │   ├── LLMProvider.swift
│   │   │   ├── LLMModel.swift
│   │   │   ├── PromptTemplate.swift
│   │   │   └── UsageRecord.swift
│   │   └── PDFCacheManager.swift
│   └── Keychain/
│       └── KeychainService.swift
├── Features/
│   ├── Search/
│   │   ├── SearchView.swift
│   │   └── SearchViewModel.swift
│   ├── Cards/
│   │   ├── CompactCardView.swift
│   │   ├── FullCardView.swift
│   │   ├── CardFlipModifier.swift
│   │   └── CardsViewModel.swift
│   ├── Reader/
│   │   ├── PDFReaderView.swift
│   │   └── PDFReaderViewModel.swift
│   ├── Chat/
│   │   ├── ChatView.swift
│   │   └── ChatViewModel.swift
│   ├── Library/
│   │   ├── LibraryView.swift
│   │   └── LibraryViewModel.swift
│   └── Settings/
│       ├── SettingsView.swift
│       ├── ProviderSettingsView.swift
│       ├── PromptEditorView.swift
│       ├── Doc2xSettingsView.swift
│       └── UsageStatsView.swift
├── Shared/
│   ├── Theme/
│   │   └── AppTheme.swift
│   ├── Components/
│   │   ├── TagChip.swift
│   │   └── LoadingOverlay.swift
│   └── Extensions/
└── Resources/
    └── DefaultPrompts.json
```

---

## 3. 数据模型

### Paper

```swift
@Model class Paper {
    var arxivId: String
    var title: String
    var authors: [String]
    var abstractText: String
    var categories: [String]
    var publishedDate: Date
    var pdfURL: String
    var pdfLocalPath: String?
    var isDownloaded: Bool
    var isFavorite: Bool
    var tags: [String]
    var llmInsight: String?
    var markdownContent: String?
    var markdownConvertStatus: ConvertStatus
    var markdownConvertedAt: Date?
    var createdAt: Date
}

enum ConvertStatus: String, Codable {
    case none
    case converting
    case completed
    case failed
}
```

### LLMProvider

```swift
@Model class LLMProvider {
    var id: UUID
    var name: String
    var providerType: ProviderType
    var baseURL: String
    var apiKey: String
    var customHeaders: [String: String]?
    var isEnabled: Bool
    var sortOrder: Int
    var models: [LLMModel]
    var createdAt: Date
}

enum ProviderType: String, Codable {
    case openai
    case anthropic
    case google
    case deepseek
    case openRouter
    case customOpenAI
}
```

### LLMModel

```swift
@Model class LLMModel {
    var id: UUID
    var provider: LLMProvider?
    var modelId: String
    var displayName: String
    var contextWindow: Int
    var maxOutputTokens: Int?
    var capabilities: ModelCapabilities
    var inputPricePerMToken: Double?
    var outputPricePerMToken: Double?
    var isDefault: Bool
    var isEnabled: Bool
}

struct ModelCapabilities: Codable {
    var textInput: Bool
    var textOutput: Bool
    var imageInput: Bool
    var imageOutput: Bool
    var pdfInput: Bool
    var functionCalling: Bool
    var streaming: Bool
    var jsonMode: Bool
    var reasoning: Bool
}
```

### PromptTemplate

```swift
@Model class PromptTemplate {
    var id: UUID
    var name: String
    var scene: PromptScene
    var systemPrompt: String
    var userPromptTemplate: String
    var responseLanguage: String
    var outputFormat: OutputFormat
    var temperature: Double
    var maxTokens: Int
    var boundModel: LLMModel?
    var isBuiltIn: Bool
    var sortOrder: Int
}

enum PromptScene: String, Codable {
    case globalSystem
    case insightGeneration
    case innovationExtract
    case formulaAnalysis
    case paperChat
    case translation
    case summary
    case custom
}

enum OutputFormat: String, Codable {
    case markdown
    case plainText
    case json
}
```

### ChatMessage / UsageRecord

```swift
@Model class ChatMessage {
    var paper: Paper?
    var role: String
    var content: String
    var timestamp: Date
}

@Model class UsageRecord {
    var model: LLMModel?
    var date: Date
    var inputTokens: Int
    var outputTokens: Int
    var estimatedCost: Double
    var requestType: RequestType
}

enum RequestType: String, Codable {
    case insightGeneration
    case paperChat
    case translation
    case codeExplanation
    case figureAnalysis
    case summary
}
```

---

## 4. UI/UX 设计

### 视觉风格: 现代卡片风

- 彩色丰富，阴影和圆角
- 类似 Anki/小红书风格

### Tab 导航

| Tab | 功能 |
|-----|------|
| 🔍 发现 | arXiv 搜索 + 缩略卡片结果流 |
| 📚 文库 | 已收藏/已下载论文管理 |
| 💬 对话 | 所有论文对话历史 |
| ⚙️ 设置 | LLM 配置、Prompt 模板、doc2x、用量统计 |

### 卡片系统

#### 缩略卡片（搜索结果/文库列表）

```
╭─────────────────────────────────╮
│ cs.AI                  📄 ♥    │  ← 分类 + Markdown状态 + 收藏
│ Attention Is All You Need      │  ← 标题（加粗，最多2行）
│ Vaswani et al. · 2024-01      │  ← 首作者 + 日期
│ We propose a new simple...     │  ← 摘要（1-2行）
╰─────────────────────────────────╯
```

角标说明: 📄 = 已有 Markdown, ⏳ = 转换中, 无 = 未转换

#### 完整卡片正面（点击缩略卡片 Hero 动画展开）

```
╭─────────────────────────────────────╮
│ cs.AI · cs.LG              ♥  ···  │
│                                     │
│  Attention Is All You Need          │
│                                     │
│  Vaswani, Shazeer, Parmar           │
│  et al.                             │
│                                     │
│  We propose a new simple network    │
│  architecture, the Transformer,     │
│  based solely on attention...       │
│                                     │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐  │
│  │ 📄  │ │ 📝  │ │ 💡  │ │ 💬  │  │
│  │ PDF │ │转MD │ │ 见解 │ │ 问答 │  │
│  └─────┘ └─────┘ └─────┘ └─────┘  │
│                                     │
│  2024-01-15        ⟳ 点击翻转       │
╰─────────────────────────────────────╯
```

#### 完整卡片背面（3D 翻转动画 600ms）

```
╭─────────────────────────────────────╮
│ ✦ 核心见解                   ⟳ 翻转 │
│─────────────────────────────────────│
│                                     │
│  🎯 核心贡献                         │
│  提出纯注意力机制架构，完全替代        │
│  RNN/CNN 的序列建模方式               │
│                                     │
│  📊 主要结果                         │
│  BLEU 41.0 (EN-DE), 训练成本         │
│  仅为竞品的 1/4                      │
│                                     │
│  🔗 与你的研究相关性: ████░ 80%      │
│                                     │
│─────────────────────────────────────│
│  ┌──────────┐ ┌──────────┐         │
│  │ 💡 创新点 │ │ 📐 公式解析│         │
│  └──────────┘ └──────────┘         │
│  ┌──────────┐ ┌──────────┐         │
│  │ 💬 论文  │ │ 🌐 全文   │         │
│  │   问答   │ │   翻译   │         │
│  └──────────┘ └──────────┘         │
│  ┌──────────┐ ┌──────────┐         │
│  │ 📖 展开  │ │ 🔄 重新   │         │
│  │  全文详情 │ │   生成   │         │
│  └──────────┘ └──────────┘         │
╰─────────────────────────────────────╯
```

### 背面按钮交互方式

| 按钮 | 行为 | 输出方式 |
|------|------|---------|
| 💡 创新点提取 | LLM 分析论文 novelty | 下滑面板 (Sheet) |
| 📐 公式解析 | LLM 解析关键公式 | 下滑面板 (Sheet) |
| 💬 论文问答 | 多轮对话 | 全屏页面 (NavigationStack) |
| 🌐 全文翻译 | 逐段翻译，原文/译文对照 | 全屏页面 |
| 📖 展开全文 | 论文详情 + PDF | 全屏页面 |
| 🔄 重新生成 | 重新生成核心见解 | 原地刷新 |

### 返回策略

| 展开类型 | 返回方式 | 返回后状态 |
|---------|---------|-----------|
| 下滑面板 | 下拉手势 或 点击背景 | 回到卡片背面，内容缓存保留 |
| 全屏页面 | 左上角 `< 返回` 或 右滑边缘手势 | 回到卡片背面，进度保留 |
| 原地刷新 | 无需返回 | 停留在卡片背面，内容更新 |

---

## 5. doc2x PDF→Markdown 服务

### 架构位置

```
PDF 文件 → doc2x API → Markdown → LLM（最优上下文）
                                → Markdown 预览（第二期）
```

### LLM 上下文构建优先级

1. 有 Markdown (doc2x) → 使用 Markdown（最佳质量，公式/表格保留）
2. 有 pdfInput 能力的模型 → 直接传 PDF
3. 都没有 → PDFKit 提取纯文本（降级方案）

### 设置页

```
─── 文档转换 ───
│ doc2x API Key     ••••••••      │
│ 服务端点          api.doc2x.com │
│ 自动转换          下载后自动 ▼   │ (下载后自动 / 仅手动 / 关闭)
│ 本月用量          23/100 页     │
│ 连通测试          ✅ 正常        │
```

### 实现约束

> **重要**: 实现时必须搜索最新的 doc2x API 文档，不依赖过时信息。

---

## 6. LLM 服务层

### 统一协议

```swift
protocol LLMServiceProtocol {
    func complete(messages: [Message],
                  model: LLMModel,
                  stream: Bool) -> AsyncThrowingStream<String, Error>
}
```

### 服务商实现

- OpenAIService: OpenAI 官方
- AnthropicService: Claude 系列
- GenericOpenAIService: 兼容 OpenAI 格式的中转站
- OpenRouterService: OpenRouter 聚合

### Prompt 模板系统

- 全局系统指令（注入所有请求）
- 6 个内置场景模板（见解/创新点/公式/问答/翻译/摘要）
- 变量系统: `{{title}}` `{{abstract}}` `{{full_text}}` `{{authors}}` `{{categories}}`
- 自定义模板支持
- 每个模板可绑定特定模型，优先级: Prompt绑定 > 场景默认 > 全局默认
- 参数调节: temperature / maxTokens / 响应语言 / 输出格式

---

## 7. 设置页完整结构

```
⚙️ 设置
├── LLM 服务商
│   ├── [预设] OpenAI / Anthropic / Google / DeepSeek / OpenRouter
│   └── ＋ 添加自定义服务商 (baseURL + apiKey + customHeaders)
├── Prompt 模板
│   ├── 全局系统指令
│   ├── 场景 Prompt (见解/创新点/公式/问答/翻译/摘要)
│   └── ＋ 自定义 Prompt
├── 模型分配
│   ├── 默认见解模型
│   ├── 默认对话模型
│   ├── 公式解析模型
│   └── 翻译模型
├── 文档转换 (doc2x)
│   ├── API Key
│   ├── 自动转换设置
│   └── 用量
├── 用量统计
│   ├── 按模型
│   ├── 按场景
│   ├── 时间趋势
│   └── 费用预警
└── 通用
    ├── 深色模式
    ├── PDF 缓存上限
    ├── iCloud 同步
    └── 清除缓存
```

---

## 8. 三期开发规划

### 第一期: MVP — 核心闭环

**目标:** 跑通「搜索 → 浏览 → 下载 → 转换 → AI见解」完整链路

| 模块 | 功能点 | 描述 |
|------|--------|------|
| 搜索 | 关键词搜索 | arXiv API，标题/摘要/全文检索 |
| | 分类筛选 | arXiv 分类下拉选择 |
| | 时间范围 | 最近一周/一月/三月/一年/全部 |
| | 排序 | 相关度/最新发布/最近更新 |
| | 分页加载 | 下拉触底加载，每页 20 条 |
| 缩略卡片 | 列表展示 | 标题+首作者+日期+1-2行摘要+分类 |
| | 收藏按钮 | 右上角♥ |
| | 点击展开 | Hero 动画到完整卡片 |
| 完整卡片 | 正面 | 完整信息 + 快捷按钮(PDF/转MD/见解/问答) |
| | 翻转动画 | 3D 翻转 600ms |
| | 背面 | 核心见解 + 展开全文 + 重新生成 |
| PDF | 下载管理 | 后台下载，进度显示，断点续传 |
| | 本地缓存 | App 沙盒存储 |
| | 阅读器 | PDFKit 基础渲染，缩放/翻页/跳页 |
| doc2x | 服务接入 | 上传PDF → 轮询状态 → 获取Markdown |
| | 手动触发 | 点击「转MD」按钮触发 |
| | 状态显示 | 转换中/已完成/失败 角标 |
| LLM | 单服务商 | OpenAI 兼容格式 |
| | 见解生成 | 混合模式: 默认arXiv摘要，点击LLM生成 |
| | 流式输出 | SSE 流式显示 |
| | 上下文构建 | 优先 Markdown → PDF直传 → 纯文本 |
| 收藏 | 收藏/取消 | 一键操作，本地持久化 |
| 文库 | 已收藏列表 | 缩略卡片展示 |
| | 已下载列表 | 已下载 PDF 的论文 |
| 设置 | API Key | LLM + doc2x 的 Key 配置 |
| | 模型选择 | 手动输入 model ID |
| 持久化 | SwiftData | Paper / ChatMessage 基础模型 |

### 第二期: LLM 全能力

**目标:** 多模型接入 + 卡片背面全部功能 + Prompt 模板体系 + 深度阅读辅助

| 模块 | 功能点 | 描述 |
|------|--------|------|
| 多LLM接入 | 预设服务商 | OpenAI / Anthropic / Google / DeepSeek |
| | OpenRouter | 内置接入，自动拉取可用模型列表 |
| | 自定义中转站 | baseURL + apiKey + customHeaders |
| | 模型能力检测 | ModelCapabilities 决定可用功能按钮 |
| | 连通性测试 | 一键 ping 测试 |
| 卡片背面全功能 | 💡 创新点提取 | 下滑面板，按重要性排列 |
| | 📐 公式解析 | 下滑面板，提取关键公式通俗解释 |
| | 💬 论文问答 | 全屏对话，基于论文的多轮问答 |
| | 🌐 全文翻译 | 全屏页面，逐段翻译，原文/译文对照 |
| | 📖 展开全文 | 全屏详情页 |
| | 🔄 重新生成 | 支持切换模型后重新生成 |
| Prompt模板 | 全局系统指令 | 注入所有请求的基础人设 |
| | 6个内置场景 | 见解/创新点/公式/问答/翻译/摘要 |
| | 变量系统 | {{title}} {{abstract}} {{full_text}} 等 |
| | 自定义模板 | 用户创建新场景 |
| | Prompt编辑器 | System + User 模板 + 参数调节 |
| | 预览测试 | 选论文测试 Prompt 效果 |
| 模型分配 | 场景绑定 | 每个场景独立绑定模型 |
| | Prompt级绑定 | 单个模板可覆盖场景默认 |
| PDF增强 | 选中文本菜单 | 复制/翻译/解释/提问 |
| | 书签 | PDF 内添加书签 |
| doc2x增强 | 自动转换 | 下载后自动提交转换 |
| | Markdown预览 | 查看转换后的 Markdown 内容 |
| | 公式解析联动 | 基于 Markdown 中的 LaTeX 公式 |
| 用量统计 | 按模型 | 每个模型 token 消耗和费用 |
| | 按场景 | 各场景占比 |
| | 时间维度 | 日/周/月趋势图 |
| | 费用预警 | 月度预算上限提醒 |

#### 论文问答上下文策略

- 短论文 (< context window 50%): 全文注入 system prompt
- 长论文 (> context window 50%): 按 section 分段，关键词匹配相关段落
- 多模态模型 (imageInput=true): 支持引用论文图表提问
- pdfInput 模型 (如 Gemini): 直接发送 PDF

### 第三期: 体验增强

**目标:** 多设备同步 + 高效浏览模式 + 智能推荐 + 全面个性化

| 模块 | 功能点 | 描述 |
|------|--------|------|
| iCloud同步 | SwiftData+CloudKit | 元数据/收藏/标签/Prompt 跨设备同步 |
| | PDF选择性同步 | 用户选择哪些 PDF 同步到 iCloud |
| | Markdown同步 | doc2x 转换结果同步 |
| | 冲突解决 | 最新修改时间优先，收藏取并集 |
| 滑动浏览 | 左右滑动 | 类似 Tinder，一次聚焦一张完整卡片 |
| | 手势操作 | 右滑=收藏，左滑=跳过，上滑=展开详情 |
| | 模式切换 | 列表/滑动一键切换 |
| | 已浏览管理 | 跳过的论文进入「已浏览」，30天后自动清理 |
| 标签系统 | 创建标签 | 自定义名称+颜色 |
| | 批量操作 | 长按多选，批量添加/移除标签 |
| | 标签筛选 | 文库页按标签筛选 |
| | 智能建议 | LLM 基于论文内容建议标签 |
| 搜索增强 | 搜索历史 | 记录最近搜索，一键重搜 |
| | 推荐主题 | 基于搜索历史推荐 |
| | 保存搜索(订阅) | 常用搜索条件保存 |
| | 定期推送 | BGAppRefreshTask 检查新论文，本地通知 |
| 深色模式 | 跟随系统 | 自动深色/浅色 |
| | 手动切换 | 设置页覆盖 |
| | PDF暗色 | PDF 阅读器独立暗色模式 |
| PDF高级 | 侧边AI面板 | 右滑呼出，边读PDF边对话 |
| | 注释系统 | 高亮标注+文字注释 |
| | 大纲导航 | 解析PDF目录，侧边快速跳转 |
| 阅读统计 | 阅读时长 | 每篇论文阅读时间 |
| | 热力图 | 日历视图展示每日阅读量 |
| | 周报/月报 | 自动生成阅读总结 |
| doc2x增强 | 批量转换 | 批量选择论文提交转换 |
| | 转换用量统计 | 统计转换页数和用量 |

---

## 9. 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| UI 框架 | SwiftUI | iOS 17+ |
| 数据持久化 | SwiftData | 替代 Core Data |
| PDF 渲染 | PDFKit | 系统原生 |
| 网络层 | URLSession + async/await | 无需第三方 |
| arXiv API | arXiv REST API | XML 格式，XMLParser 解析 |
| LLM 通信 | OpenAI 兼容 HTTP API | 统一协议层 |
| PDF→MD | doc2x API | 实现时搜索最新文档 |
| 密钥存储 | Keychain | API Key 安全存储 |
| 云同步 | CloudKit + SwiftData | 第三期接入 |
| 动画 | SwiftUI Animation | 卡片翻转/Hero 过渡 |
| 最低版本 | iOS 17.0 | SwiftData 要求 |

---

## 10. 实现约束

1. **所有第三方 API 实现时必须搜索最新文档**，不依赖过时信息。涉及的服务包括:
   - doc2x (PDF→Markdown)
   - OpenRouter (模型聚合)
   - OpenAI API
   - Anthropic API
   - Google Gemini API
   - DeepSeek API
   - arXiv API
2. **实现阶段使用 Agent 团队共建**，并行开发各模块
3. MVP 先跑通核心闭环，逐步迭代
4. 项目目录: `/Users/xiaodongzheng/exps/ArxivLearner/`
